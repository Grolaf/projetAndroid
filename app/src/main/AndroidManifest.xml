<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.scolastic">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Scolastic">
        <activity android:name=".activty_profile_page"></activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".InscriptionActivity" />
        <activity android:name=".MenuMatieresActivity" />
        <activity android:name="exercices.ExerciceCalculActivity" />
        <activity android:name="exercices.ErreurActivity" />
        <activity android:name="exercices.FelicitationsActivity" />
    </application>

</manifest>